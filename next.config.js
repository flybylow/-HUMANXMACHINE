/** @type {import('next').NextConfig} */
const nextConfig = {
  // Uncomment the lines below when ready to deploy as static site
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
}

module.exports = nextConfig

